// Feature flag for remote API usage
export const USE_REMOTE_API = false;

// Static data URLs (relative to public path)
export const STATIC_DATA = {
  categories: '/api/categories.json',
  products: '/api/products.json'
};
